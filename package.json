{
  "name": "koa-ctx-basic-auth",
  "version": "1.0.0",
  "description": "Augments Koa with ctx.basicAuth",
  "main": "lib",
  "scripts": {
    "lint": "standard lib/*.js test/*.js",
    "test": "mocha test",
    "test-cov": "nyc --cache node_modules/mocha/bin/_mocha test && nyc report --reporter=lcov"
  },
  "author": "Jonathan Ong <me@jongleberry.com> (http://jongleberry.com)",
  "license": "MIT",
  "repository": "koajs/ctx-basic-auth",
  "dependencies": {
    "basic-auth": "^1.0.4"
  },
  "devDependencies": {
    "koa": "^2.0.0",
    "mocha": "^3.1.2",
    "nyc": "^7.0.0",
    "standard": "^8.6.0",
    "supertest": "^2.0.1"
  },
  "files": [
    "lib"
  ],
  "keywords": [
    "koa",
    "basic",
    "auth",
    "context",
    "request"
  ]
}
